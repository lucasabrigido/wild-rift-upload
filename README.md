# README #

## Step 1: configs

```SHELL
$ yarn serverless deploy --cfg {STAGE} --aws-profile {PROFILE} --config ./resources/resources-upload-table.yml
$ yarn serverless deploy --cfg {STAGE} --aws-profile {PROFILE} --config ./resources/resources-file-bucket.yml
$ yarn serverless deploy --cfg {STAGE} --aws-profile {PROFILE}
```
